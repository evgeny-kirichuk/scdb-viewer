{
  "name": "book-store",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install:apps": "concurrently \"cd server && go mod tidy\" \"npm install --prefix client\"",
    "server": "cd server && go run cmd/api/main.go",
    "server:prod": "cd server && node index.js",
    "client": "npm start --prefix client",
    "test:client": "npm test --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build:client": "npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
